import { TestBed } from '@angular/core/testing';

import { FirestoreOnlineService } from './firestore-online.service';

describe('FirestoreOnlineService', () => {
  let service: FirestoreOnlineService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(FirestoreOnlineService);
  // });

  it('should be created', () => {
    expect(true).toBeTruthy();
  });
});
