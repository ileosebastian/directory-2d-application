import { ComponentFixture, TestBed, async } from '@angular/core/testing';
import { DataOptionsPage } from './data-options.page';

describe('DataOptionsPage', () => {
  let component: DataOptionsPage;
  let fixture: ComponentFixture<DataOptionsPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(DataOptionsPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
