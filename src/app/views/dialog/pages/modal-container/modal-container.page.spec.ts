import { ComponentFixture, TestBed, async } from '@angular/core/testing';
import { ModalContainerPage } from './modal-container.page';

describe('ModalContainerPage', () => {
  let component: ModalContainerPage;
  let fixture: ComponentFixture<ModalContainerPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(ModalContainerPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
