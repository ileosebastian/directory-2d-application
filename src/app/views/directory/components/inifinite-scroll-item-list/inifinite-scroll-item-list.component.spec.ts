import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { InifiniteScrollItemListComponent } from './inifinite-scroll-item-list.component';

describe('InifiniteScrollItemListComponent', () => {
  let component: InifiniteScrollItemListComponent;
  let fixture: ComponentFixture<InifiniteScrollItemListComponent>;

  // beforeEach(waitForAsync(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ InifiniteScrollItemListComponent ],
  //     imports: [IonicModule.forRoot()]
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(InifiniteScrollItemListComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
