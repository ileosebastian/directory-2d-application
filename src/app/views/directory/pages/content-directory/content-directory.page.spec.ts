import { ComponentFixture, TestBed, async } from '@angular/core/testing';
import { ContentDirectoryPage } from './content-directory.page';

describe('ContentDirectoryPage', () => {
  let component: ContentDirectoryPage;
  let fixture: ComponentFixture<ContentDirectoryPage>;

  // beforeEach( async(() => {
  //   fixture = TestBed.createComponent(ContentDirectoryPage);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
