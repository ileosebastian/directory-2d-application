import { ComponentFixture, TestBed, async } from '@angular/core/testing';
import { ProfessorSearchPage } from './professor-search.page';

describe('ProfessorSearchPage', () => {
  let component: ProfessorSearchPage;
  let fixture: ComponentFixture<ProfessorSearchPage>;

  // beforeEach(async(() => {
  //   fixture = TestBed.createComponent(ProfessorSearchPage);
  //   component = fixture.componentInstance;
  //   fixture.autoDetectChanges();
  // }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
